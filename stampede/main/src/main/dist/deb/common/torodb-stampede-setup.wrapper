#!/bin/bash

. "/etc/default/@{assembler.name}"

export TOROCONFIG
export TOROPASSFILE
export TOROCMD="/usr/share/@{assembler.name}/bin/@{assembler.name}"
export INTERACTIVE=true
export TORO_USER=torodb

exec "/usr/share/@{assembler.name}/bin/@{assembler.name}-setup" "$@"