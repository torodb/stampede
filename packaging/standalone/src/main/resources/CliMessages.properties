help=Print help and exit.
print-config=Print the configuration in YAML format and exit.
print-xml-config=Print the configuration in XML format and exit.
help-param=Print help for all available parameters and exit.
help-param-header=To specify a parameter use the syntax <path>=<value> where:\n\
		<path> follow JSON pointer format (http://tools.ietf.org/html/draft-ietf-appsawg-json-pointer-03) with the exception that you can omit the first '/' character.\n\
		<value> have the same syntax as values in YAML 1.1 format (http://yaml.org/spec/1.1/).\n\
		Possible paths are:\n\

conf=Configuration file in YAML format.
xml-conf=Configuration file in XML format.
backend=Specify the backend to use with default values.
param=Specify a configuration parameter using <path>=<value> syntax. Use --help-param to see all available parameters.
ask-for-password=Force input of PostgreSQL's database user password.